{
  "name": "create-biome",
  "version": "1.0.0",
  "bin": {
    "create-biome": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsdown build src/index.ts --outDir dist",
    "dev": "tsdown watch src/index.ts --outDir dist",
    "lint": "biome check .",
    "lint:fix": "biome format --write . && biome check --write ."
  },
  "files": [
    "dist/",
    "src/biome.template.json"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@biomejs/cli-darwin-arm64": "^2.3.7",
    "@clack/prompts": "^0.11.0",
    "@types/node": "^24.10.1",
    "chalk": "^5.6.2",
    "commander": "^14.0.2",
    "tsdown": "^0.16.6",
    "typescript": "^5.6.0"
  }
}